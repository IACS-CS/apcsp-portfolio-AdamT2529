import { renderMenu } from "../utils/renderMenu";

export function renderCitations () {
  /* html */
  return `
  <h1>Citations</h1>
  <p>Initial template from my teacher Mr. Hinkle</
  p>
  <p>Used Copilot to help with... </p>
  <p>(later fill in libraries you use etc.)<p>
  <p>(later fill in media citations)<p>
  `;
}